videobridge {
    http-servers {
        public {
            port = 9090
        }
    }
    websockets {
        enabled = true
        domain = "{{ domains | first }}:443"
        tls = true
    }
}
