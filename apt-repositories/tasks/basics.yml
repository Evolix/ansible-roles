---

- name: Change basics repositories
  template:
    src: basics.list.j2
    dest: /etc/apt/sources.list
    mode: "0644"
    force: yes
    backup: yes
  notify: apt update

- name: Intermediate flush of handlers
  meta: flush_handlers
