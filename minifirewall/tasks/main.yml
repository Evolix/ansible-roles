---

- name: packages are installed
  apt:
    name: '{{ item }}'
    state: present
  with_items:
  - iptables

- include: install.yml

- include: config.yml

- include: activate.yml

- include: tail.yml
  when: minifirewall_tail_included
